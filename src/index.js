import React from "react";
import ReactsDOM from "react-dom/client";
import App from "./App";

const el = document.getElementById('root');
const root = ReactsDOM.createRoot(el);

root.render(<App />);